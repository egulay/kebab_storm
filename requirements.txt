# --- Spark stack ---
# Use PySpark 4.x on Python >=3.9; fall back to a 3.x LTS on Python <3.9
pyspark==4.0.1; python_version >= "3.9"
pyspark==3.4.3; python_version < "3.9"

# NumPy wheels vary by Python version:
numpy==2.0.2; python_version < "3.10" and python_version >= "3.9"
numpy==1.24.4; python_version < "3.9"          # (e.g., Python 3.8)
numpy==2.2.6; python_version == "3.10"
numpy==2.3.3; python_version >= "3.11"

# --- Date/time & serialization ---
python-dateutil==2.9.0.post0
pytz==2025.2
PyYAML==6.0.2
protobuf==6.32.1

# --- CLI / config helpers ---
docopt==0.6.2
confuse==2.0.1

# --- HTTP stack (pin only top-level) ---
requests==2.32.3

# --- Security / misc ---
pycryptodome==3.23.0
validators==0.35.0
yarg==0.1.10

# Legacy helper (remove if unused)
six==1.16.0